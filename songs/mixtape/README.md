# Mixtape staging folder

Put any tracks you want mirrored to Backblaze B2 inside this folder. The upload script (`scripts/upload_mixtape.sh`) will sync it to your configured bucket so you can reuse the URLs when wiring up the on-site music player.
